

export default function CartProduct({product,product:{image,id,price,title,size,count},setCarts}) {
    // const decrease =() =>{
    //     if(count <= 1)return;
    //     setCarts((carts) =>
    //         carts.map((cart) => {cart.id === id ? {...product,count:count -1} : cart})
    //     )
    // }
    // const increase = () =>{
    //     setCarts((carts) => carts.map((cart) => {cart.id === id}))
    // }
    return(
        <section></section>
    )
}